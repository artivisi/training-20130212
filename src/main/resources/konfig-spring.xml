<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- tidak perlu declare masing-masing dao, cukup autodetect anotasi @Repository -->
    <context:component-scan base-package="com.artivisi.training.dao" />
                
    <!-- konfigurasi dikeluarkan ke file jdbc.properties -->
    <context:property-placeholder location="classpath:jdbc.properties" /> 

    <!-- nilai konfigurasi menggunakan variabel yang didefinisikan di jdbc.properties -->
    <bean id="ds" class="org.postgresql.ds.PGSimpleDataSource">
        <property name="serverName" value="${database.host}"/>
        <property name="databaseName" value="${database.name}"/>
        <property name="user" value="${database.username}"/>
        <property name="password" value="${database.password}"/>
    </bean>
    
</beans>